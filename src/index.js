const json=(d,s=200)=>new Response(JSON.stringify(d,null,2),{status:s,headers:{"content-type":"application/json; charset=utf-8"}});
const clean=(v,n=500)=>String(v||"").trim().slice(0,n);
export function createPlan(body={}){
  const subject=clean(body.subject),audience=clean(body.audience),goal=clean(body.goal),script=clean(body.script,8000);
  if(subject.length<4||audience.length<3||goal.length<3)return{error:"Subject, audience, and goal are required."};
  const aspect=["9:16","16:9","1:1"].includes(body.aspect)?body.aspect:"9:16";const duration=Math.min(180,Math.max(15,Number(body.duration)||45));const clipDuration=Math.min(10,Math.max(2,Number(body.clipDuration)||5));const count=Math.ceil(duration/clipDuration);
  const beats=["Hook","Problem","Context","Method","Proof","Action"];
  const storyboard=Array.from({length:count},(_,i)=>{const start=i*clipDuration,end=Math.min(duration,(i+1)*clipDuration),beat=beats[Math.min(beats.length-1,Math.floor(i/count*beats.length))];return{shot:i+1,time:`${start}-${end}s`,beat,visual:`${beat} visual for ${subject}`,searchTerms:[subject,beat.toLowerCase(),audience].filter(Boolean),caption:i===0?subject:beat}});
  return{version:"0.1.0",status:"planning_only",subject,audience,goal,aspect,duration,script:script||`Open with a direct hook about ${subject}. Explain the problem for ${audience}, show a practical method, provide proof, and close with ${goal}.`,storyboard,rendering:{supported:false,reason:"Video rendering requires a separate isolated Python/FFmpeg job runtime.",recommendedRuntime:"Containerized MoneyPrinterTurbo worker with queue, object storage, limits, and rights review."},moneyPrinterTurbo:{video_subject:subject,video_script:script,video_aspect:aspect,video_clip_duration:clipDuration,video_count:1,video_source:"pexels",subtitle_enabled:true,bgm_type:"random",paragraph_number:1},rightsChecklist:["Confirm rights for every stock clip and music track.","Confirm voice and model provider terms.","Review generated claims and likenesses before publishing.","Delete source and rendered media according to the storage policy."],provenance:"Planning schema adapted from harry0703/MoneyPrinterTurbo under MIT."};
}
const page=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>AI Video Studio</title><style>:root{--bg:#0d1117;--ink:#f6f2e9;--muted:#a4abb4;--accent:#ff5c35;--panel:#161c24;--line:#303845}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:16px/1.5 Arial,sans-serif}main{max-width:1180px;margin:auto;padding:48px 22px 80px}header{display:grid;grid-template-columns:1.15fr .85fr;gap:50px;align-items:end;padding:40px 0 48px;border-bottom:1px solid var(--line)}h1{font-size:clamp(3.3rem,8vw,7rem);line-height:.87;letter-spacing:-.075em;margin:0}.muted{color:var(--muted)}form{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-top:38px}label{display:block;font-weight:700}input,select,textarea{width:100%;margin-top:7px;background:var(--panel);color:var(--ink);border:1px solid var(--line);border-radius:11px;padding:13px;font:inherit}textarea{min-height:150px}.wide{grid-column:1/-1}button{border:0;border-radius:999px;background:var(--accent);color:#fff;padding:14px 22px;font-weight:800;width:max-content}.notice{border-left:4px solid var(--accent);padding:14px 18px;background:var(--panel);margin:30px 0}.shots{display:grid;grid-template-columns:repeat(2,1fr);gap:1px;background:var(--line)}.shot{background:var(--bg);padding:18px}.shot b{color:var(--accent)}@media(max-width:720px){header,form{grid-template-columns:1fr}.wide{grid-column:auto}.shots{grid-template-columns:1fr}}</style></head><body><main><header><h1>AI Video Studio</h1><p class="muted">Plan a short video, create a shot-by-shot storyboard, and export a renderer-ready manifest. This release does not render or upload media.</p></header><div class="notice"><b>Planning mode:</b> no model keys, uploads, stock downloads, or video files are processed here.</div><form id="f"><label>Subject<input name="subject" required placeholder="How AI agents change local business"></label><label>Audience<input name="audience" required placeholder="Local business owners"></label><label>Goal<input name="goal" required placeholder="Book an AI workflow audit"></label><label>Aspect<select name="aspect"><option>9:16</option><option>16:9</option><option>1:1</option></select></label><label>Duration in seconds<input name="duration" type="number" min="15" max="180" value="45"></label><label>Clip duration<input name="clipDuration" type="number" min="2" max="10" value="5"></label><label class="wide">Optional script<textarea name="script" placeholder="Leave blank for a structured outline"></textarea></label><button>Create production plan</button></form><div id="out" aria-live="polite"></div></main><script>const out=document.querySelector('#out');document.querySelector('#f').onsubmit=async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const r=await fetch('/api/plan',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(d.error){out.innerHTML='<p>'+d.error+'</p>';return}window.plan=d;out.innerHTML='<div class="notice"><b>'+d.subject+'</b><br>'+d.duration+' seconds · '+d.aspect+' · '+d.storyboard.length+' shots</div><h2>Storyboard</h2><div class="shots">'+d.storyboard.map(x=>'<div class="shot"><b>'+x.time+' · '+x.beat+'</b><p>'+x.visual+'</p><small>'+x.searchTerms.join(' / ')+'</small></div>').join('')+'</div><p><button id="dl">Download manifest</button></p><p class="muted">'+d.rendering.reason+'</p>';document.querySelector('#dl').onclick=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(d,null,2)],{type:'application/json'}));a.download='video-plan.json';a.click();URL.revokeObjectURL(a.href)}};</script></body></html>`;
export async function routeRequest(req){const u=new URL(req.url);if(u.pathname==="/health")return json({status:"ok",service:"jae-ai-video-studio",version:"0.1.0",mode:"planning_only"});if(u.pathname==="/api/plan"&&req.method==="POST"){const b=await req.json().catch(()=>({}));const r=createPlan(b);return json(r,r.error?400:200)}if(u.pathname==="/"&&req.method==="GET")return new Response(page,{headers:{"content-type":"text/html; charset=utf-8"}});return json({error:"Not found"},404)}export default{fetch:routeRequest};
